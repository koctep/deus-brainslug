import template from './slug-mind.html';
import controller from './slug-mind.controller';
import './slug-mind.scss';

let slugMindComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller,
  controllerAs: 'vm'
};

export default slugMindComponent;
